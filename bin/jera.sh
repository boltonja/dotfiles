#!/bin/sh
cat <<EOF
            > > >
              > > >
                > > >
         < < <    > > >
       < < <    > > >
     < < <    > > >
       < < <
         < < <
           < < <
EOF
exit
cat <<EOF
            > > >
              > > >
       < < <    > > >
     < < <        > > >
   < < <        > > >
 < < <        > > >
   < < <    > > >
     < < < 
       < < < 
EOF
